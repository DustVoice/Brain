---
publish: true
created: 2025-06-26 16:09
---


A cross-shell prompt toolkit, similar to [[Starship]].

I use it with [[fish]], as it seems to be smoother and with better integration.

## Install

### [[Fedora]]

> [!todo] Install
> - [[unzip]]

Make sure `~/bin` or `~/.local/bin` available and in your `$PATH`, then:

```sh
curl -s https://ohmyposh.dev/install.sh | bash -s 
```

Everything else should be included in my [[Dotfiles]].
